## 1. HTML 구조 추가

- [x] 1.1 홈 화면(screen-home)에 "계산기" 버튼 추가 (점수 이력 버튼 아래)
- [x] 1.2 계산기 화면(screen-calculator) 섹션 추가: 디스플레이 영역(식 표시 + 결과 표시 + 나머지 표시), 4×4 버튼 그리드, 홈으로 버튼

## 2. CSS 스타일 추가

- [x] 2.1 계산기 버튼 스타일 추가 (btn-calculator, 기존 btn-history와 유사한 패턴)
- [x] 2.2 계산기 디스플레이 스타일 추가 (식 표시 영역, 메인 숫자 영역, 나머지/에러 메시지 영역)
- [x] 2.3 4×4 버튼 그리드 스타일 추가 (숫자 버튼, 연산자 버튼, C 버튼, = 버튼 각각 구분)
- [x] 2.4 반응형 스타일 추가 (태블릿 768px, 모바일 500px 대응)

## 3. 계산기 로직 구현 (app.js)

- [x] 3.1 계산기 상태 변수 선언 (displayValue, firstOperand, operator, waitingForSecond)
- [x] 3.2 홈 화면 "계산기" 버튼 클릭 → screen-calculator 전환 이벤트
- [x] 3.3 숫자 버튼 클릭 핸들러 (0~9 입력, 3자리 제한, 선행 0 처리)
- [x] 3.4 연산자 버튼 클릭 핸들러 (firstOperand 저장, 연산자 변경 지원)
- [x] 3.5 = 버튼 핸들러 (사칙연산 계산, 정수 결과, 나머지 표시)
- [x] 3.6 에러 처리 (0 나누기 → "0으로 나눌 수 없어요", 결과 > 999 → "너무 커요!", 결과 < 0 → "0보다 작아요!")
- [x] 3.7 C 버튼 핸들러 (전체 초기화)
- [x] 3.8 디스플레이 업데이트 함수 (식 표시 + 메인 숫자 + 나머지/에러 메시지)
- [x] 3.9 사운드 연동 (버튼 클릭 → Sound.click(), 결과 표시 → Sound.correct())
- [x] 3.10 홈으로 버튼 이벤트 연결
